name: rspress-deploy
run-name: Github Actions Deploy
on:
  push:
    branches: ['main']

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Check deploy token
        run: |
          if [ -z "${{ secrets.TOKEN }}" ]; then
            echo "::error::请在仓库 Settings → Secrets and variables → Actions 中添加 TOKEN（PAT，需具备 xSeek-Neo/docs 的 repo 写权限）"
            exit 1
          fi

      - name: Init new repo in dist folder and commit generated files
        run: |-
          cd dist
          git init
          git add .
          git config --local user.email "352671309@qq.com"
          git config --local user.name "Ellis"
          git config --local init.defaultBranch main
          git commit -am 'deploy'

      - name: Force push to destination branch
        uses: ad-m/github-push-action@v0.8.0
        with:
          github_token: ${{ secrets.TOKEN }}
          branch: main
          force: true
          repository: xSeek-Neo/docs
          directory: ./dist
